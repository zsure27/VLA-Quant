# 字节相同的源码快照

本目录每份文件名带原始字节 SHA256 前 12 位。它们替代多个分片里字节完全相同的 `evaluator-source.py`、`oft-evaluator-source.py` 和 `probe-source.py`。全部旧路径、完整哈希和字节数见[去重映射](../../indexes/DEDUPLICATED_FILES_20260922.json)。与当前维护版 `qvla/` 或 `diagnostics/` 恰好相同不代表未来版本也相同，因此这里保留独立不可变快照。

历史 `SHA256SUMS.txt` 仍描述当时完整服务器归档的路径；原版可从清理前 Git 提交或会话大文件归档恢复。本目录不改变旧测量。
